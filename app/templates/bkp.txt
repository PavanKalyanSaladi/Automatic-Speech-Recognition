<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Automatic Speech Recognition</title>
    <script src="/static/js/recorder.js" defer></script>
    <style>
        .container {
            display: grid;
            grid-template-columns: 1fr 1fr; /* Creates two equal columns */
            gap: 20px; /* Optional: Space between sections */
        }
        body {
            background-color: #FF7F50; /* Lavendar background */
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding-left: 100px;
            min-height: 100vh;
            margin: 0;
            color: #333;
        }
        h1 {
            color: #F5F5DC; /* Charcoal */
            margin-bottom: 120px;
            font-size: 40px;
        }
        section {
            background-color: #ffffff; /* White background for sections */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 10px;
            width: 80%;
            max-width: 600px;
            text-align: center;
        }
        h2 {
            color: #000000; /* Black */
            margin-bottom: 15px;
        }
        button {
            padding: 10px 10px;
            border: none;
	    border-radius: 80%;
	    width: 60px;
	    height: 45px;
	    align-items: center;
	    justify-content: center;
            cursor: pointer;
	    margin: 5px;
        } /*
	button {
	     width: 50px;
	     height: 50px;
	     border-radius: 50%;
	     background-color: #007bff;
	     color: white;
	     padding: 10px 10px;
	     border: none;
	     display: flex;
	     align-items: center;
	     justify-content: center;
	     font-size: 16px;
	     cursor: pointer;
	} */
	#pause-resume, #stop {
            visibility: hidden; /* Hide the buttons initially */
        }
        #record {
            background-color: #388e3c; /* Green */
            color: white;
	    visibility: visible;
        }
        #stop {
            background-color: #FF0000; /* Red */
            color: white;
        }
        #upload-button {
            background-color: #388e3c; /* Green */
            color: white;
        }
        audio {
            margin-top: 10px;
        }
        #record-result, #upload-result {
            margin-top: 15px;
            font-size: 16px;
            color: #555;
	    font-weight: bold
        }
    </style>
</head>
<body>
    <h1>Welcome to Automatic Speech Recognition</h1>
    <br>
    <div class="container">
        <!-- Audio Recording -->
        <section>
            <h2>Audio Conversion</h2>
	    <button id="record">Record</button>
		    <!--<img src='./record.png'/ alt="Record"></button>-->
	    <button id="pause-resume">Pause</button>
            <button id="stop">Stop</button>
            <audio id="audio" controls></audio>
	    <br><br>
            <div id="recordingStatus"></div>
	    <div id="record-result"></div>
        </section>

        <!-- File Upload -->
        <section>
            <h2>File Conversion</h2>
            <form id="upload-form">
                <input type="file" id="audio-file" name="audioFile" accept="audio/*" />
                <button type="button" id="upload-button">Convert</button>
		<audio id="audio2" controls></audio>
                <div id="upload-result"></div>
            </form>
        </section>
    </div>
</body>
</html>
